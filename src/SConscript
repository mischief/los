import os.path
Import(['env'])

# , 'com'
subdirs = [
'crt', 'kern', 'lmm',
'clientos', 'libc',
'posix',
'com',
'dev',
#'linux',
'diskpart',
'memdebug'
]

Mkdir("#lib")

SConscript([os.path.join(i, 'SConscript') for i in subdirs], exports = ['env'])
